title: Deadline Pro
description: A private, offline date calculator and deadline tracker for iOS.

# REQUIRED for GitHub Pages project sites — paths break without this.
# Site is served at https://raihaniabd.github.io/deadline-pro/
baseurl: "/deadline-pro"

exclude:
  - README.md

defaults:
  - scope:
      path: ""
    values:
      layout: default
